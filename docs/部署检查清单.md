# 生产环境部署检查清单

## 📋 部署前检查

### 服务器准备
- [ ] 服务器已准备（CPU 2核+, 内存 4GB+, 硬盘 50GB+）
- [ ] 操作系统已安装（Ubuntu 20.04+ 推荐）
- [ ] 域名已备案（如需要）
- [ ] DNS 已解析到服务器 IP

### 域名和证书
- [ ] 管理后台域名：`cs-admin.yourdomain.com`
- [ ] 后端 API 域名：`cs-api.yourdomain.com`
- [ ] 玩家端域名：`cs-player.yourdomain.com`
- [ ] SSL 证书已申请（Let's Encrypt 或云服务商）

### 软件安装
- [ ] Node.js 18.x 或 20.x
- [ ] PostgreSQL 14.x+
- [ ] Redis 6.x+
- [ ] Nginx 1.18+
- [ ] PM2 或 Docker

---

## 🔐 安全配置

### 密钥生成
使用以下命令生成所有密钥，并记录下来：

```bash
# 生成 6 个不同的密钥
for i in {1..6}; do echo "密钥$i: $(openssl rand -base64 32)"; done

# 生成加密密钥
echo "加密密钥: $(openssl rand -hex 32)"
```

- [ ] JWT_SECRET（密钥1）
- [ ] WS_TOKEN_SECRET（密钥2）
- [ ] UPLOAD_TOKEN_SECRET（密钥3）
- [ ] SESSION_TOKEN_SECRET（密钥4）
- [ ] METRICS_KEY（密钥5）
- [ ] ENCRYPTION_SALT（密钥6）
- [ ] ENCRYPTION_SECRET_KEY（加密密钥）

### 数据库安全
- [ ] 创建专用数据库用户（不使用 postgres）
- [ ] 设置强密码（至少 16 位）
- [ ] 限制数据库远程访问（如不需要）

---

## ⚙️ 配置文件修改

### backend/.env
```bash
# 必须修改的配置
DATABASE_URL="postgresql://cs_app:强密码@localhost:5432/cs_prod_db?schema=public"
JWT_SECRET="生成的密钥1"
REDIS_URL="redis://localhost:6379"
NODE_ENV="production"
PORT=21101
LOG_LEVEL="INFO"
FRONTEND_URL="https://cs-admin.yourdomain.com"
WS_URL="wss://cs-api.yourdomain.com"
DIFY_API_KEY="生产环境的key"
DIFY_BASE_URL="https://your-dify-domain.com/v1"
ENCRYPTION_SECRET_KEY="生成的加密密钥"
ENCRYPTION_SALT="生成的密钥6"
WS_TOKEN_SECRET="生成的密钥2"
UPLOAD_TOKEN_SECRET="生成的密钥3"
SESSION_TOKEN_SECRET="生成的密钥4"
METRICS_KEY="生成的密钥5"
```

- [ ] DATABASE_URL 已修改
- [ ] 所有密钥已替换
- [ ] NODE_ENV 改为 production
- [ ] FRONTEND_URL 改为生产域名
- [ ] WS_URL 改为 wss://
- [ ] Dify 配置已更新

### frontend/admin-portal/.env
```bash
VITE_API_BASE_URL=https://cs-api.yourdomain.com/api/v1
VITE_WS_URL=wss://cs-api.yourdomain.com
```

- [ ] API 地址已修改为生产域名
- [ ] WebSocket 地址使用 wss://

### webview-player/.env（可选）
```bash
VITE_API_URL=https://cs-api.yourdomain.com
```

- [ ] API 地址已配置（或通过 URL 参数传递）

---

## 🗄️ 数据库初始化

```bash
cd backend

# 1. 执行迁移
npx prisma migrate deploy

# 2. 创建初始管理员
npm run seed
```

- [ ] 数据库迁移已执行
- [ ] 初始管理员账号已创建
- [ ] 默认密码已记录（admin / Admin@123456）

---

## 🏗️ 构建项目

### 后端
```bash
cd backend
pnpm install
pnpm build
```
- [ ] 后端依赖已安装
- [ ] 后端已构建成功

### 管理后台
```bash
cd frontend/admin-portal
pnpm install
pnpm build
```
- [ ] 管理后台依赖已安装
- [ ] 管理后台已构建成功

### 玩家端
```bash
cd webview-player
pnpm install
pnpm build
```
- [ ] 玩家端依赖已安装
- [ ] 玩家端已构建成功

---

## 🚀 部署服务

### 方式 A: PM2 部署
```bash
cd backend
pm2 start ecosystem.config.js --env production
pm2 save
pm2 startup
```
- [ ] PM2 已启动后端
- [ ] PM2 已保存配置
- [ ] PM2 开机自启已配置

### 方式 B: Docker 部署
```bash
docker-compose up -d
```
- [ ] Docker 服务已启动
- [ ] 容器运行正常

---

## 🌐 Nginx 配置

### 配置文件
- [ ] `/etc/nginx/sites-available/cs-api` 已创建
- [ ] `/etc/nginx/sites-available/cs-admin` 已创建
- [ ] `/etc/nginx/sites-available/cs-player` 已创建
- [ ] 软链接已创建到 sites-enabled
- [ ] SSL 证书路径已配置
- [ ] 域名已替换为实际域名

### 前端文件部署
```bash
sudo mkdir -p /var/www/admin-portal
sudo mkdir -p /var/www/webview-player
sudo cp -r frontend/admin-portal/dist/* /var/www/admin-portal/
sudo cp -r webview-player/dist/* /var/www/webview-player/
sudo chown -R www-data:www-data /var/www/admin-portal
sudo chown -R www-data:www-data /var/www/webview-player
```
- [ ] 前端文件已复制到 Nginx 目录
- [ ] 文件权限已设置

### 启动 Nginx
```bash
sudo nginx -t
sudo systemctl restart nginx
```
- [ ] Nginx 配置测试通过
- [ ] Nginx 已重启

---

## ✅ 功能验证

### 基础服务检查
```bash
# 后端健康检查
curl https://cs-api.yourdomain.com/api/v1/health

# PostgreSQL
psql -U cs_app -d cs_prod_db -c "SELECT version();"

# Redis
redis-cli ping

# Nginx
sudo systemctl status nginx
```
- [ ] 后端 API 响应正常
- [ ] 数据库连接正常
- [ ] Redis 连接正常
- [ ] Nginx 运行正常

### 管理后台测试
1. 访问 `https://cs-admin.yourdomain.com`
2. 使用默认账号登录（admin / Admin@123456）
3. 修改管理员密码
4. 添加测试游戏

- [ ] 管理后台可访问
- [ ] 登录功能正常
- [ ] 密码已修改
- [ ] 可以添加游戏

### 玩家端测试
1. 访问 `https://cs-player.yourdomain.com?apiUrl=https://cs-api.yourdomain.com`
2. 测试创建工单
3. 测试发送消息
4. 测试图片上传

- [ ] 玩家端可访问
- [ ] 可以创建工单
- [ ] 可以发送消息
- [ ] 图片上传正常

### WebSocket 测试
```bash
npm install -g wscat
wscat -c "wss://cs-api.yourdomain.com/socket.io/?EIO=4&transport=websocket"
```
- [ ] WebSocket 连接成功
- [ ] 可以收发消息

---

## 🔧 监控和维护

### 日志配置
- [ ] 日志轮转已配置
- [ ] 日志目录权限正确
- [ ] 可以查看实时日志

### 备份配置
```bash
# 数据库备份脚本
sudo nano /usr/local/bin/backup-cs-db.sh
sudo chmod +x /usr/local/bin/backup-cs-db.sh

# 定时任务
sudo crontab -e
# 添加: 0 3 * * * /usr/local/bin/backup-cs-db.sh
```
- [ ] 数据库备份脚本已创建
- [ ] 定时备份已配置
- [ ] 备份目录已创建

### SSL 证书自动续期
```bash
# 测试续期
sudo certbot renew --dry-run

# 定时任务
sudo crontab -e
# 添加: 0 2 * * * certbot renew --quiet --post-hook "systemctl reload nginx"
```
- [ ] 证书自动续期测试通过
- [ ] 定时续期任务已配置

### 监控工具
```bash
# 安装监控工具
sudo apt install htop iotop nethogs

# PM2 监控
pm2 monit
```
- [ ] 系统监控工具已安装
- [ ] PM2 监控可用

---

## 🔒 安全加固

### 防火墙配置
```bash
# UFW 防火墙
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP
sudo ufw allow 443/tcp   # HTTPS
sudo ufw enable
```
- [ ] 防火墙已启用
- [ ] 只开放必要端口

### SSH 安全
```bash
# 禁用密码登录，使用密钥
sudo nano /etc/ssh/sshd_config
# PasswordAuthentication no
sudo systemctl restart sshd
```
- [ ] SSH 密钥登录已配置
- [ ] 密码登录已禁用（可选）

### 系统更新
```bash
sudo apt update
sudo apt upgrade -y
```
- [ ] 系统已更新到最新

---

## 📝 最终检查

### 配置确认
- [ ] 所有 localhost 已改为生产域名
- [ ] 所有 http:// 已改为 https://
- [ ] 所有 ws:// 已改为 wss://
- [ ] 所有示例密钥已替换
- [ ] NODE_ENV 为 production
- [ ] 日志级别为 INFO 或 WARN

### 安全确认
- [ ] 默认管理员密码已修改
- [ ] 数据库密码足够强
- [ ] 所有密钥已重新生成
- [ ] CORS 配置正确
- [ ] 防火墙已配置

### 功能确认
- [ ] 管理后台可正常使用
- [ ] 玩家端可正常使用
- [ ] WebSocket 连接正常
- [ ] 文件上传功能正常
- [ ] AI 对话功能正常

### 运维确认
- [ ] 日志可正常查看
- [ ] 数据库备份已配置
- [ ] SSL 证书自动续期已配置
- [ ] 监控工具已安装
- [ ] 文档已归档

---

## 🎉 部署完成

恭喜！系统已成功部署到生产环境。

### 下一步
1. 通知游戏服务器团队更新配置
2. 提供玩家端接入文档
3. 培训客服人员使用系统
4. 监控系统运行状态
5. 定期检查日志和备份

### 重要提醒
- 定期检查 SSL 证书有效期
- 定期检查数据库备份
- 定期更新系统和依赖
- 关注系统性能指标
- 及时处理错误日志

---

**检查清单版本**: v1.0  
**最后更新**: 2026-01-21
